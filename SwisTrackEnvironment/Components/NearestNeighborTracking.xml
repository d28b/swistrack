<?xml version="1.0"?>
<swistrackcomponent>
	<friendlyname>Nearest Neighbor Tracking</friendlyname>
	<description>Extremely simple tracking component; takes pre-filtered particles (use the BlobSelection and BlobDetection components before this one) and adds each of them to the track nearest to it. Make sure that the max number of tracks and the max number of particles in BlobDetection are the same.</description>
	<url>http://en.wikibooks.org/wiki/SwisTrack/Components/NearestNeighborTracking</url>
	<displayimage>Output</displayimage>
	<configuration>
	    <parameter type="integer">
		    <name>MaxNumber</name>
			<label>Max Number</label>
			<unit>Individuals</unit>
			<default>10</default>
		</parameter>
	</configuration>
</swistrackcomponent>
